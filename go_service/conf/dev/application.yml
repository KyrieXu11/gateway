server:
  port: 8080

log:
  level: info

gin:
  mode: debug

session:
  max_age: 20

rpc:
  matcher:
    server_addr: "127.0.0.1:9000"

whitelist:
  - "/**/login"
  - "/**/register"
  - "/**.js"
  - "/**.css"
  - "/**.html"